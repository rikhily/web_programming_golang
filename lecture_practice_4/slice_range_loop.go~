package main

import "fmt"

type Residence struct {
	name string
	city string
}

func main() {
	
	user_slice := []Residence{ {"Him", "Fresno"}, 
				   {"Her", "Clovis"},
				   {"They", "Sanger"},}

	for _, value := range user_slice{
		fmt.Println(value)
	}
}
